import React from 'react'
import { Card, CardContent, Typography } from '@material-ui/core'

const Cards = ({data}) => {
    console.log(data)
    return (
        <div>
           
        </div>
    )
}

export default Cards
